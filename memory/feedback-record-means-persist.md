---
name: record-means-persist
description: 说"已记录/已保存"时必须实际写入文件，不能仅口头确认
type: feedback
---

当我对用户说「已记录」「已保存」时，必须立即将信息写入文件（memory、plan 或对应配置），不能只做口头确认。

**Why:** 用户曾因我说「已记录」但实际未写入文件而产生困惑，要求此类错误不能再出现。

**How to apply:** 任何需要持续生效的偏好、规则或上下文，都应调用 Write/Edit 等工具写入持久化文件，并在完成后再口头告知用户。